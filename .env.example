# chapchu-api BFF origin. OAuth login adds the current FE /auth/callback as redirect.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
T_MAP_APIKEY=replace-with-your-tmap-api-key

# Server-only. Store the decoded data.go.kr service key locally; never use NEXT_PUBLIC_.
KMA_SERVICE_KEY=replace-with-your-decoded-kma-service-key
